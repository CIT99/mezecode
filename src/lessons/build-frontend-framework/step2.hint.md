## Hint

To create a DOM element, use `document.createElement(tagName)`.
To set attributes, use `element.setAttribute(key, value)` or `element[key] = value`.
To append children, use `element.appendChild(child)`.
For text nodes, use `document.createTextNode(text)`.

Remember to recursively call render for nested element objects!

