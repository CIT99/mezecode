## Hint

Remember that:
- Functions can use rest parameters (`...children`) to accept multiple arguments
- Objects can be created with object literal syntax `{ key: value }`
- Default parameters can be used: `props = {}`

Try creating a function that takes three parameters: tag, props, and uses rest syntax for children.

